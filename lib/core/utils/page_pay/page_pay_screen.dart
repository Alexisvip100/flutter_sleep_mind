import 'package:flutter/material.dart';

class PagePayScreen extends StatelessWidget {
  const PagePayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(child: Column(children: [
      Text('hola')
    ],),);
  }
}